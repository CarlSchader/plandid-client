const categoriesSet = new Set([0, 1, 2, 3, 4, 5, 6]);

const millisecondMap = {
    week: 604800000,
    day: 86400000,
    hour: 3600000,
    minute: 60000,
    second: 1000
};

export {
    categoriesSet,
    millisecondMap
}